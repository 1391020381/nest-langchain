{
  "name": "@autix/langgraph-learning",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "demo": "bun run src/main.ts",
    "demo:stream": "bun run src/main.ts --stream",
    "demo:hitl": "bun run src/hitl-demo.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@autix/llm-core": "workspace:*",
    "@langchain/core": "^1.2.4",
    "@langchain/langgraph": "^1.4.8",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
