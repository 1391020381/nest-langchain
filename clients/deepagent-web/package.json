{
  "name": "@autix/deepagent-web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3100",
    "build": "next build",
    "start": "next start --port 3100",
    "typecheck": "tsc -p tsconfig.validate.json --noEmit",
    "lint": "tsc -p tsconfig.validate.json --noEmit",
    "test": "bun test lib"
  },
  "dependencies": {
    "@autix/deepagent-contracts": "workspace:*",
    "next": "16.2.3",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5.7.0"
  }
}
